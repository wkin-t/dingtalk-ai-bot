# DingTalk AI Bot 领域上下文

这个上下文描述机器人多后端文本生成链路中的路由、容错和联网搜索术语。它只记录领域语言，不记录模块实现、环境变量或部署命令。

## Gemini 路由与容错

**主路径**：当前请求通常优先使用的 Gemini 文本生成服务路径，由 antigravity 账号池承载。
_避免_: 把主路径等同于某个固定模型名；主路径是服务路径，模型名可以随配置变化。

**保底路径**：当主路径在请求建立阶段不可用时，用来完成同一次文本请求的 Vertex 服务路径。
_避免_: 把保底路径称为“重试模型”；它是另一条服务路径，模型兼容性由 Vertex 侧 alias 或可选覆盖保证。

**熔断**：在一段冷却窗口内停止探测已知不可靠的主路径，让新请求直接使用保底路径；窗口结束后再恢复主路径探测。

**账号池故障**：主路径服务暂时无法提供可用上游账号的 provider 故障。当前策略不依赖某个固定错误文本，建流阶段的 provider 异常都属于可触发熔断的故障。

## 联网搜索

**原生搜索**：目标模型通过自身 SDK 提供的搜索工具完成联网，搜索请求和模型回答属于同一次模型调用。

**搜索摘要 fallback**：独立调用 Gemini 搜索后，把摘要拼接进另一个模型的 system message。它是历史兼容路径，不是原生搜索；当前默认关闭但保留显式回滚能力。
