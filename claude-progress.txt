## 最近完成
- 多轮 thinking 两条路径全部接通
  - TD1 (OpenRouter): _is_complete_reasoning 修复类型识别
    旧 filter 只匹配 thinking/reasoning，实际 SDK 产出 reasoning.encrypted/text/summary，
    导致 vacuous truth 残体可漏入下一轮。新增 7 个 normalized type 用例 + round-trip 集成测试
  - TD2 (OpenAI Responses): 接入 previous_response_id
    新增 app/responses_state.py（Redis + 文件降级，TTL 7 天）
    _stream_via_responses 改造：有 prev_id 时仅发末位 user 消息 + previous_response_id；
    无则发全量历史；store=True 始终开启；失效自动清状态回退重试一次
    clear_cutoff.set_cutoff/reset_cutoff 同步清 response_id，避免 /clear /resume 状态不一致
  - 测试: 394 passed (新增 TD1: 9 个；TD2: 6 + 2 + 4 个)

## 当前正在做
- 待部署验证

## 下一步应做
- 三容器重建 + 在线观察 thinking 跨轮保持

## 已知问题
- 多个 pre-existing Pyright 类型告警（非本次引入）

## 环境状态
- Git 分支: master
- 当前 HEAD: 4f991ff (fix: bot 显示名称按实际模型推断)
- 服务状态: 三容器已重建并验证启动正常（2026-05-21）
- 测试状态: 372 passed, compile OK

=== 最近 10 条 commit ===
f55baef refactor: 完全移除 LiteLLM，迁移所有路径到官方 SDK
c9a0669 fix: 修复路由分析失败导致 thinking_text 丢失的两处根因
277393d fix: litellm openrouter 路径加 use_responses_api=False + drop_params=True
bbcfffa fix: openrouter_client 去掉 socks5 代理注入，中转站路径直连即可
fba6dbf fix: 补充 openrouter>=0.9.1 依赖，修复阶段 3 Docker 构建缺包
cb2129f feat: 阶段 3 — OpenRouter 官方 SDK 替代 LiteLLM + USE_LEGACY_LITELLM 回退开关
fb33032 feat: LLM SDK 规范化 + Thinking Blocks 多轮保留（阶段 1+2）
fc31640 fix: 将 OpenRouter 路径的 cache_control 注入改为 flatten，修复 Responses API 不兼容
840b86d fix: 移除 OpenRouter 路径的 use_responses_api=False
6aad3d5 fix: litellm openai_api_base 路径加 drop_params=True，静默丢弃中转站不支持的参数
