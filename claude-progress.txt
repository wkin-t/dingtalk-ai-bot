## 最近完成（2026-07-02）
- 多后端原生联网搜索上线 + 部署补齐（本地=GitHub=服务器=161c3f6，2026-07-02 二次核实+补部署：
  此前服务器曾停在 c8af69c 缺 fallback 加固，已 git pull + 三容器 up -d --build 补齐并进容器 grep 验证）
  - 三容器全部走 sub2api 中转 + 原生搜索工具，取代旧的"自己 google 搜完拼进 prompt"
    - gemini: GEMINI_API_BASE 指向 sub2api /v1beta，google_search 透传 groundingMetadata 回流；
      拆 direct_client 让生图端点仍直连 Google（GEMINI_API_KEY 语义不变）
    - openai(gpt-5.5): Responses API web_search（GA 名，非 web_search_preview）
    - openrouter: AI_BACKEND=openai 走 sub2api，模型改 anthropic/claude-sonnet-5，
      web_search 被翻译成 OpenRouter web plugin(Exa)
    - 三容器 env 开 OPENAI_FLASH/PRO_SUPPORTS_SEARCH=true；均端到端真实代码路径验证联网成功
  - google_search fallback 加固（161c3f6，已部署）：独立 GEMINI_SEARCH_MODEL（非 MODEL_ROUTER，
    避免 gpt/claude 名发给 Gemini 接口 404）+ 流式（规避 Py3.14 非流式代理报错）+ SEARCH_TIMEOUT_SECONDS 超时
  - 测试: 413 passed（2026-07-02 新会话本地真实复跑确认）
- 多 agent 设计审查：43 个确认 finding（详见 memory/service-audit-findings.md）

## 最近完成（2026-07-02 续）
- 全自主搜索上线（commit bd3fa37，已部署三容器）：SEARCH_AUTONOMOUS=true（默认）时
  app/ai/backend.py::resolve_enable_search 在统一入口对 fast/pro 档强制挂原生搜索工具、
  模型自决是否搜索，取代路由 need_search 关键词门控。豁免：lite 档、openclaw、openai 后端
  的 gemini 上游与 supports_search=False 模型（后两者强开会落 fallback 注入烧私人号）。
  服务器 .env 未设该键 = 走代码默认 True，已随部署生效（容器内 grep resolve_enable_search=3）。
  （核实注：交接文档称 SEARCH_AUTONOMOUS "已加 config.py:294" 为误——本会话才真正写入）
- finding #2 修复（commit 6c5527d，已部署+端到端验证）：/v1/chat/completions 补 Bearer 鉴权。
  服务器三容器 .env 已写同一随机 token；实测无 token/错误 token 三端口全 401，正确 token 放行转发。
- 多 agent 审查本次 diff：3 确认（均非阻塞）+ 3 驳回。关键驳回：红队核实 sub2api Claude 路径
  实际把 web_search 翻译成 Anthropic 原生 web_search（模型自决、挂载不计费），而非 progress/CLAUDE.md
  记录的 "OpenRouter web plugin(Exa) 每请求付费"——该记录存疑，但红队未提供 sub2api 源码直证，待核实。

## 当前正在做 / 待决策
- 🌐 图标语义漂移（medium，未修）：全自主后每条 fast/pro 回复都常显 🌐（挂了工具就显示，
  不代表本次真搜了）。gemini 路径可改成检测 groundingMetadata 出现才置 icon；GPT 路径
  sub2api 丢弃 web_search_call 无信号，只能改图标语义。等用户决定是否要修。
- 费用观察：全自主已开，用户可持续观察 Google 后台 grounding 用量；若超预算再加每日上限保护。

## 下一步应做
- 观察全自主搜索线上表现（模型是否更愿意联网）+ grounding 费用
- 其余未处理 finding（见 memory/service-audit-findings.md）：#15 XFF 伪造、#9/#18 顶层 genai.Client
  import 崩溃、#11/#13/#21/#22 企微+openclaw/openai 路由解包错位

## 已知问题
- 多个 pre-existing Pyright 类型告警（非本次引入）
- openai/openrouter 容器 fallback 极端降级时仍会用私人 gemini 号（未设 GEMINI_API_BASE）

## 环境状态
- Git 分支: master | 本地=GitHub=服务器=6c5527d（2026-07-02 部署后核实，三容器 RestartCount=0）
- 服务器 git 已配 socks 代理: http.https://github.com/.proxy socks5h://127.0.0.1:1080
- 三容器: gemini(35000)/openai(35001)/openrouter(35002) 均 RestartCount=0 稳定运行
- 测试: 436 passed, compile OK

## 关键坑（详见 memory/deploy-env-pitfalls.md）
- env 重复键后者覆盖前者：改 env 后用 `docker-compose config | grep VAR` 看解析后最终值
- runtime ghost /app/.env 让 load_dotenv 偶发覆盖 docker env：用 up -d --build 清掉
- 改 env 后必须 up -d（--build），docker restart/force-recreate 可能不生效或给时序假象

=== 最近 commit ===
6c5527d fix: /v1/chat/completions 补 Bearer 鉴权（finding #2 高危：零鉴权开放 API 代理）
bd3fa37 feat: 全自主搜索——fast/pro 档强制挂原生搜索工具，模型自决（lite 豁免）
38dde7d docs: 进度更新——核实部署状态 + 161c3f6 fallback 加固补部署记录
161c3f6 feat: 多后端原生联网搜索 + Gemini 中转 + 搜索 fallback 加固
